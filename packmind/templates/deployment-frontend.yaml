{{- if .Values.frontend.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "packmind.fullname" . }}-frontend
  labels:
    {{- include "packmind.frontend.labels" . | nindent 4 }}
  {{- with (include "packmind.annotations" .) }}
  annotations:
    {{- if . }}
    {{- . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  {{- if not .Values.frontend.autoscaling.enabled }}
  replicas: {{ .Values.frontend.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "packmind.frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/secrets: {{ include (print $.Template.BasePath "/secrets.yaml") . | sha256sum }}
        {{- with .Values.frontend.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with (include "packmind.annotations" .) }}
        {{- if . }}
        {{- . | nindent 8 }}
        {{- end }}
        {{- end }}
      labels:
        {{- include "packmind.frontend.selectorLabels" . | nindent 8 }}
        {{- with .Values.frontend.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- include "packmind.imagePullSecrets" . | nindent 6 }}
      {{- include "packmind.serviceAccountNameField" . | nindent 6 }}
      {{- include "packmind.podSecurityContextField" (dict "context" . "component" "frontend") | nindent 6 }}
      containers:
        - name: frontend
          {{- include "packmind.securityContextField" (dict "context" . "component" "frontend") | nindent 10 }}
          image: "{{ .Values.frontend.image.repository }}:{{ include "packmind.imageTag" (dict "tag" .Values.frontend.image.tag "context" .) }}"
          imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.frontend.service.targetPort }}
              protocol: TCP
          env:
            - name: NGINX_ENV
              value: "K8S"
            - name: INGRESS_ENABLED
              value: {{ .Values.ingress.enabled | quote }}
            {{- if not .Values.ingress.enabled }}
            # Service discovery for no-ingress mode (nginx proxies to backend services)
            - name: API_SERVICE_HOST
              value: "{{ include "packmind.fullname" . }}-api"
            - name: API_SERVICE_PORT
              value: {{ .Values.api.service.port | quote }}
            - name: MCP_SERVICE_HOST
              value: "{{ include "packmind.fullname" . }}-mcp-server"
            - name: MCP_SERVICE_PORT
              value: {{ .Values.mcpServer.service.port | quote }}
            {{- end }}
            {{- include "packmind.allEnvVars" (dict "component" "frontend" "context" .) | nindent 12 }}
          {{- with .Values.frontend.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.frontend.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
          volumeMounts:
            {{- include "packmind.serviceVolumeMounts" (dict "component" "frontend" "context" .) | nindent 12 }}
      volumes:
        {{- include "packmind.serviceVolumes" (dict "component" "frontend" "context" .) | nindent 8 }}
      {{- with .Values.frontend.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.frontend.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.frontend.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
