{{- if .Values.redis.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "packmind.fullname" . }}-redis-data
  labels:
    {{- include "packmind.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
  annotations:
    {{- if .Values.redis.persistence.resourcePolicy }}
    "helm.sh/resource-policy": {{ .Values.redis.persistence.resourcePolicy | quote }}
    {{- end }}
    {{- with (include "packmind.annotations" .) }}
    {{- if . }}
    {{- . | nindent 4 }}
    {{- end }}
    {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  # Explicitly specify volume mode for clarity
  volumeMode: Filesystem
  {{- if .Values.redis.persistence.storageClass }}
  storageClassName: {{ .Values.redis.persistence.storageClass | quote }}
  {{- else if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass | quote }}
  {{- end }}
  {{- /* When no storage class is set, storageClassName is omitted so the cluster's default class is used (an explicit "" disables dynamic provisioning). */}}
  resources:
    requests:
      storage: {{ .Values.redis.persistence.size | quote }}
    {{- if .Values.redis.persistence.limits }}
    limits:
      storage: {{ .Values.redis.persistence.limits.storage | quote }}
    {{- end }}
  {{- if .Values.redis.persistence.selector }}
  selector:
    {{- toYaml .Values.redis.persistence.selector | nindent 4 }}
  {{- end }}
{{- end }}
