{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "packmind.fullname" . }}-postgresql-data
  labels:
    {{- include "packmind.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
  annotations:
    # Skip resource management for existing PVCs to avoid immutability conflicts
    "helm.sh/resource-policy": "keep"
    {{- with (include "packmind.annotations" .) }}
    {{- if . }}
    {{- . | nindent 4 }}
    {{- end }}
    {{- end }}
spec:
  accessModes:
    - ReadWriteOnce
  # Explicitly specify volume mode for clarity
  volumeMode: Filesystem
  {{- if or .Values.postgresql.persistence.storageClass (not .Values.postgresql.persistence.storageClass) }}
  {{- if .Values.postgresql.persistence.storageClass }}
  storageClassName: {{ .Values.postgresql.persistence.storageClass | quote }}
  {{- else if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass | quote }}
  {{- else }}
  # Use default storage class if none specified
  storageClassName: ""
  {{- end }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.postgresql.persistence.size | quote }}
    {{- if .Values.postgresql.persistence.limits }}
    limits:
      storage: {{ .Values.postgresql.persistence.limits.storage | quote }}
    {{- end }}
  {{- if .Values.postgresql.persistence.selector }}
  selector:
    {{- toYaml .Values.postgresql.persistence.selector | nindent 4 }}
  {{- end }}
{{- end }}
